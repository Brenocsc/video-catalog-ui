import { AppBar, Toolbar } from '@mui/material';
import * as React from 'react';

export const Navbar: React.FC = () => {
  return (
    <AppBar>
        <Toolbar>
          texto
        </Toolbar>
    </AppBar>
  )
}